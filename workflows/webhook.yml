on:
  webhook:  
    method: post
jobs:
      - name: create-json
        id: create-json
        uses: DamianReeves/write-file-action
        with:
          path: ${{ env.home}}/driver.json
          contents: |
            echo "Hello World!"
          write-mode: append
